# Built binary (root only — must not match cmd/cricket-mcp/)
/cricket-mcp
*.db
*.db.gz
# ...but the tiny test fixture must ship, or CI has nothing to test
!internal/history/testdata/fixture.db
cricsheet/
.DS_Store
